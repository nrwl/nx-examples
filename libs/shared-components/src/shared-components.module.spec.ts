import { SharedComponentsModule } from './shared-components.module';

describe('SharedComponentsModule', () => {
  it('should work', () => {
    expect(new SharedComponentsModule()).toBeDefined();
  });
});
