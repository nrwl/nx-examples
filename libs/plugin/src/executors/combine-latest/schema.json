{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "CombineLatest executor",
  "description": "",
  "type": "object",
  "properties": {
    "targets": {
      "type": "array",
      "description": "The targets to run in parallel",
      "items": {
        "$ref": "#/definitions/targets"
      },
      "x-priority": "important"
    }
  },
  "definitions": {
    "targets": {
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "object",
          "properties": {
            "target": {
              "type": "string"
            },
            "overrides": {
              "type": "object",
              "additionalProperties": true
            }
          },
          "additionalProperties": false,
          "required": ["target"]
        }
      ]
    }
  },
  "required": ["targets"]
}
