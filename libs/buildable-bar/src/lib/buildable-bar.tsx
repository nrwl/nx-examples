import React from 'react';

/* eslint-disable-next-line */
export interface BuildableBarProps {}

export const BuildableBar = (props: BuildableBarProps) => {
  return (
    <div>
      <h1>Welcome to buildable-bar!</h1>
    </div>
  );
};

export default BuildableBar;
